# name of the workflow.
# this is optional.
name: All Tests

# events that will trigger this workflow.
# here, we only have "pull_request", so the workflow will run
# whenever we create a pull request.
# other examples: [push] and [pull_request, push]
on:
  pull_request:

  # push:
  #   branches:
  #     - dev

# each workflow must have at least one job.
# jobs run in parallel by default (we can change that).
# each job groups together a series of steps to accomplish a purpose.
jobs:
  deploy:
    runs-on: ubuntu-*
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Rust setup
        uses: https://github.com/dtolnay/rust-toolchain@stable

      - name: Check
        run: cargo check

      - name: All Tests
        run: cargo test --no-fail-fast --quiet

      # --- Cleanup Steps ---
      - name: Cleanup Rust Artifacts
        if: always()
        run: |
          echo "Cleaning up Cargo build artifacts and cache..."

          # Clean the workspace target directory completely
          cargo clean

          # Clean the global cargo registry cache inside the runner to save space
          rm -rf ~/.cargo/registry/cache/
          rm -rf ~/.cargo/registry/src/

          echo "Cleanup complete."
